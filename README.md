# 🗺️ แผนที่พิกัดสแกมเมอร์ลาว - Survey Map

แผนที่ interactive สำหรับติดตามการสำรวจพิกัดสแกมเมอร์ในลาว

## 📁 โครงสร้างไฟล์

```
scammer_map/
├── index.html      # หน้าเว็บแผนที่
├── data.json       # ข้อมูลพิกัด (แก้ไขไฟล์นี้เพื่ออัปเดต)
└── README.md       # คู่มือการใช้งาน
```

## 🚀 วิธี Deploy บน GitHub Pages

### ขั้นตอนที่ 1: สร้าง Repository

```bash
# สร้างโฟลเดอร์และ git init
cd scammer_map
git init
git add .
git commit -m "Initial commit"
```

### ขั้นตอนที่ 2: Push ขึ้น GitHub

```bash
# สร้าง repo ใหม่บน GitHub แล้ว
git remote add origin https://github.com/YOUR_USERNAME/scammer-map.git
git branch -M main
git push -u origin main
```

### ขั้นตอนที่ 3: เปิด GitHub Pages

1. ไปที่ Repository Settings
2. เลือก Pages (เมนูซ้าย)
3. Source: Deploy from a branch
4. Branch: main / root
5. Save

รอ 2-3 นาที แล้วเข้าได้ที่: `https://YOUR_USERNAME.github.io/scammer-map/`

---

## 📝 วิธีอัปเดตข้อมูล (แก้ไข data.json)

### โครงสร้าง data.json

```json
{
  "locations": [
    {
      "id": 1,
      "name": "L1-Patumphon",
      "lat": 14.795122,
      "lng": 105.938579,
      "frequency": 749,
      "bank": "BAY",
      "province": "อุบล/ติดเวียดนาม",
      "area": "ชป.4",
      "team": "บอม พล",
      "google_maps": "https://maps.app.goo.gl/xxx",
      
      "survey_status": "สำรวจแล้ว",
      "survey_date": "2025-01-10",
      "surveyor": "ทีม ชป.4",
      "lac": "51234",
      "ci": "12345678",
      
      "ais": {"dbm": "-75", "status": "ใช้ได้ดี"},
      "dtac": {"dbm": "-82", "status": "ใช้ได้ (อ่อน)"},
      "true": {"dbm": "", "status": "ไม่มีสัญญาณ"},
      
      "images": [
        "https://drive.google.com/file/d/xxx/view",
        "https://drive.google.com/file/d/yyy/view"
      ],
      "videos": [
        "https://drive.google.com/file/d/zzz/view"
      ],
      "notes": "พบอาคาร 3 ชั้น มีกล้องวงจรปิดรอบ"
    }
  ],
  "last_updated": "2025-01-11"
}
```

### ค่าที่ต้องกรอกหลังสำรวจ

| Field | คำอธิบาย | ตัวอย่าง |
|-------|----------|----------|
| `survey_status` | สถานะสำรวจ | `"ยังไม่สำรวจ"`, `"อยู่ระหว่างสำรวจ"`, `"สำรวจแล้ว"` |
| `survey_date` | วันที่สำรวจ | `"2025-01-10"` |
| `surveyor` | ชื่อผู้/ทีมสำรวจ | `"ทีม ชป.4"` |
| `lac` | Location Area Code | `"51234"` |
| `ci` | Cell ID | `"12345678"` |
| `ais.dbm` | ค่าสัญญาณ AIS | `"-75"` |
| `ais.status` | สถานะ AIS | `"ใช้ได้ดี"`, `"ใช้ได้ (อ่อน)"`, `"ไม่มีสัญญาณ"` |
| `dtac.dbm` | ค่าสัญญาณ DTAC | `"-82"` |
| `dtac.status` | สถานะ DTAC | เหมือน AIS |
| `true.dbm` | ค่าสัญญาณ TRUE | `"-90"` |
| `true.status` | สถานะ TRUE | เหมือน AIS |
| `images` | Array ลิงก์รูปภาพ | `["https://drive.google.com/file/d/xxx/view"]` |
| `videos` | Array ลิงก์วิดีโอ | `["https://drive.google.com/file/d/yyy/view"]` |
| `notes` | หมายเหตุ | `"พบอาคารน่าสงสัย"` |

---

## 🖼️ วิธีเพิ่มรูปภาพ/วิดีโอจาก Google Drive

### ขั้นตอนที่ 1: อัปโหลดไฟล์ไป Google Drive

### ขั้นตอนที่ 2: ตั้งค่าการแชร์
1. คลิกขวาที่ไฟล์ → Share
2. เลือก "Anyone with the link"
3. Copy link

### ขั้นตอนที่ 3: ใส่ลิงก์ใน data.json

```json
"images": [
  "https://drive.google.com/file/d/1ABC123xyz/view?usp=sharing"
],
"videos": [
  "https://drive.google.com/file/d/2DEF456abc/view?usp=sharing"
]
```

> 💡 **Tips**: ระบบจะแปลงลิงก์เป็น thumbnail อัตโนมัติ

---

## ✏️ วิธีแก้ไขและ Push ขึ้น GitHub

### วิธีที่ 1: แก้ไขผ่าน GitHub Web (ง่ายสุด)

1. ไปที่ Repository บน GitHub
2. คลิกที่ `data.json`
3. คลิกไอคอน ✏️ (Edit)
4. แก้ไขข้อมูล
5. คลิก "Commit changes"
6. รอ 1-2 นาที แผนที่จะอัปเดตอัตโนมัติ

### วิธีที่ 2: แก้ไขในเครื่อง

```bash
# ดึงข้อมูลล่าสุด
git pull

# แก้ไข data.json ด้วย text editor

# Commit และ Push
git add data.json
git commit -m "อัปเดตข้อมูลสำรวจ L1-L5"
git push
```

---

## 🎨 สีหมุดบนแผนที่

| สี | ความหมาย |
|----|----------|
| 🟢 เขียว | สำรวจแล้ว |
| 🟡 เหลือง | อยู่ระหว่างสำรวจ |
| 🔴 แดง | ยังไม่สำรวจ |
| 🟣 ม่วง | พื้นที่กว้าง (A1-A6) |

---

## 🔧 Troubleshooting

### รูปไม่แสดง
- ตรวจสอบว่าไฟล์ถูก Share เป็น "Anyone with the link"
- ลองเปลี่ยน `/view` เป็น `/preview` ในลิงก์

### JSON Error
- ตรวจสอบ comma ระหว่าง items
- ใช้ [JSONLint](https://jsonlint.com/) ตรวจสอบ syntax

### แผนที่ไม่อัปเดต
- Clear cache (Ctrl+Shift+R)
- รอ GitHub Pages rebuild (1-2 นาที)

---

## 📞 Contact

สร้างโดย: [ชื่อทีม]  
อัปเดตล่าสุด: 2025-01-11
